import type { Metadata } from 'next';
import InfoPage, { Checklist } from '@/components/InfoPage';

export const metadata: Metadata = { title: 'Methodology' };
export default function MethodologyPage() { return <InfoPage eyebrow="Transparent calculations" title="How TaxCalc turns gross pay into take-home pay." intro="Each supported country has a separate, versioned engine. TaxCalc never uses a generic European formula or silently borrows rules from another country."><h2>Gross to net</h2><p>The engine annualises the selected pay period, applies country-specific taxable-pay treatment, calculates progressive tax slices, credits or allowances, statutory social contributions, supported pension treatment and user-entered deductions. Money is handled in integer minor units and country-relevant rounding is applied.</p><h2>Net to gross</h2><p>TaxCalc uses a bounded monotonic binary-search solver. It repeatedly runs the complete gross-to-net engine until the calculated net pay matches the target within one minor unit. It never divides by a guessed average tax rate.</p><h2>Annual estimate versus payslip</h2><p>Income tax can be cumulative and social contributions can use payroll-period thresholds. The current product provides a salary-comparison estimate; UK NI uses monthly thresholds and Irish PRSI uses an annualised weekly calculation including the 2026 effective-date change.</p><h2>Maintenance workflow</h2><Checklist items={['Check official budget and tax-authority documentation', 'Update versioned parameters and effective dates', 'Run boundary and reverse-calculation tests', 'Compare official worked examples where available', 'Record source URLs and the verification date']} /><h2>Known limits</h2><p>The core engines model standard employment income. They do not cover self-employment, multiple jobs, foreign tax credits, complex share awards, special expatriate reliefs, non-resident tax, capital gains or rental income.</p></InfoPage>; }
